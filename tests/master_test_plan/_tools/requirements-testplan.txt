# Dev tools needed by Layer 1 static analysis slices.
# Install into the project venv with:
#   pip install -r tests/master_test_plan/_tools/requirements-testplan.txt

# Layer 01-01 — ruff lint sweep
ruff>=0.4.0

# Layer 01-02 — bandit security scan
bandit>=1.7.0

# Layer 01-03 — vulture dead-code detection
vulture>=2.11

# Layer 01-06 — radon complexity metrics
radon>=6.0.1

# Layer 01-07 — pydeps (circular import detection)
pydeps>=1.12

# Layer 01-05 — mypy (type-annotation sweep on public APIs only)
mypy>=1.8.0
